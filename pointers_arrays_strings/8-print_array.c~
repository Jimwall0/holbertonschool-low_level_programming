#include "main.h"
/**
 * print_array - prints a array
 * @a: the array
 * @n: the amount to be printed
 * Return: void
 */
void print_array(int *a, int n)
{
int i;
for (i = 0; i < n; i++)
{
putchar(a[i]);
}
}
