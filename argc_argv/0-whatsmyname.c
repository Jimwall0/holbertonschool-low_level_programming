#include <stdio.h>
/**
 * main - should run the program
 * Return: int
 * @argc: int holding the length of the string
 * @argv: array
 */
int main(int argc, char *argv[])
{
(void) argc;
printf("%s\n", argv[0]);
return (0);
}
