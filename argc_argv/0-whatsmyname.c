#include <stdio.h>
/**
 * main - should run the program
 * Return: int
 * @argc - int holding the length of the string
 * @argv - array
 */
int main(int argc, char *argv[])
{
printf("%s\n", argv[0]);
return (argc);
}
