#include "main.h"
/**
 * factorial - Write a function that returns the factorial of a given number
 * @n: the number
 * Return: the factoorial
 */
int factorial(int n)
{
  int num = 1;
  if ()
    {
      num++;
      n = n * factorial(n - num);
    }
  return (n);
}
