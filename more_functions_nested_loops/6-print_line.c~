#include "main.h"
/**
 * print_line - prints a line across the screen
 * @n: how long the line is
 */
void print_line(int n)
{
int num;
for (num = 0; num <= n; num++)
{
if (n < 0)
{
_putchar('-');
}
else
{
_putchar('-');
}
}
}
